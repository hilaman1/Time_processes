import numpy as np


def calcRate(spikeTrain, window):
    """ receive a spike train and length of the window """
    if (window == 0):
        rateOfFire = np.mean(spikeTrain)
    wt = 1 / np.sqrt(2*np.pi*)
    rateOfFire =
    return rateOfFire
